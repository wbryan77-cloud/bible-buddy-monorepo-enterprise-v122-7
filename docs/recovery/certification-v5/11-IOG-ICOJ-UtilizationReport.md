# 11 — IOG/ICOJ Utilization Report

Approved IOG/ICOJ **cross-references** (`data/approved-cross-references.jsonl`, 135 AUTO_APPROVED) are reachable on the `bible_wide` authority path via `readApprovedCrossReferences`.

They are **not** primary witnesses and **not** raw sermon prose in answers.

Evidence Cards / catalog remain the OpenAI composer approved pack layer.

**Gate E1:** PARTIAL — inventory + live wiring proven; per-answer contribution telemetry incomplete.

See `11-IOG-ICOJ-UtilizationMatrix.json`.
