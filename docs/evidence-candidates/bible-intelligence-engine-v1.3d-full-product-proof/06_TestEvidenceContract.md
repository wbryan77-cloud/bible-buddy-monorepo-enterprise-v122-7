# 06 — Test Evidence Contract

Every production probe records: id, family, SHA, input, reply, pass/fail, latency where measured, owner/route when available.
Raw logs: `founder-corpus-postdeploy.log`, `memory-cert-run.log`, `final-production-proof.json`, `answer-proof-results.json`.
Schema: `test-evidence-schema.json`.
